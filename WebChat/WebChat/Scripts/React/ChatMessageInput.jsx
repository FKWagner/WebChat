var ChatMessage = React.createClass({
    render: function() {
        return (
          <div className="form-group">
              // Need Form here
          </div>
      );
    }
});
ReactDOM.render(
  <ChatMessage />,
  document.getElementById('ChatMessage')
);
